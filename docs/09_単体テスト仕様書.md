# 単体テスト仕様書
YouTube分析バッチシステム

## 1. テスト方針
- pytest使用
- 外部I/O（API）はモック化

### 1.1 カバレッジ目標

| 対象 | 目標 | 備考 |
|-----|------|------|
| 全体 | 80%以上 | Line Coverage基準 |
| ビジネスロジック | 90%以上 | sentiment/, aggregate/ |
| I/O層 | 70%以上 | fetch/（モック前提） |

**除外対象**: 設定ファイル、`if __name__ == "__main__"` ブロック

## 2. テスト実行

### 2.1 全テスト実行
```bash
docker compose exec app pytest
```

### 2.2 単一ファイル実行
```bash
docker compose exec app pytest tests/test_analyzer.py -v
```

### 2.3 カバレッジ付き実行
```bash
docker compose exec app pytest --cov=app --cov-report=html
```

## 3. テストケース

### 3.1 感情分析（sentiment/analyzer.py）

| No | テスト内容 | 入力 | 期待出力 |
|----|-----------|------|---------|
| 1 | ポジティブ判定 | "最高！とても面白かった" | positive > 0.5 |
| 2 | ネガティブ判定 | "つまらない、時間の無駄" | negative > 0.5 |
| 3 | ニュートラル判定 | "動画を見ました" | neutral が最大 |
| 4 | 空文字 | "" | 均等分布 |
| 5 | 絵文字のみ | "👍👍👍" | positive > neutral |

```python
# tests/test_analyzer.py
from unittest.mock import patch

def test_classify_positive():
    result = classify_comment("最高！とても面白かった")
    assert result["positive"] > 0.5
    assert "language" in result

def test_classify_negative():
    result = classify_comment("つまらない、時間の無駄")
    assert result["negative"] > 0.5

def test_classify_empty():
    result = classify_comment("")
    # 空文字は均等分布に近い
    assert abs(result["positive"] - result["negative"]) < 0.3
```

### 3.2 コメント取得（fetch/youtube.py）

| No | テスト内容 | 入力 | 期待出力 |
|----|-----------|------|---------|
| 1 | デフォルト件数 | video_id="abc123" | len(comments) <= 10 |
| 2 | 件数指定 | video_id="abc123", limit=5 | len(comments) <= 5 |
| 3 | 存在しない動画 | video_id="invalid" | [] |
| 4 | APIエラー | (403 quota exceeded) | 例外発生 |

```python
# tests/test_fetch.py
from unittest.mock import patch, MagicMock

@patch('fetch.youtube.build')
def test_fetch_comments_default_limit(mock_build):
    mock_response = {'items': [{'snippet': {...}} for _ in range(10)]}
    mock_build.return_value.commentThreads.return_value.list.return_value.execute.return_value = mock_response

    comments = fetch_comments("abc123")
    assert len(comments) <= 10

@patch('fetch.youtube.build')
def test_fetch_comments_custom_limit(mock_build):
    mock_response = {'items': [{'snippet': {...}} for _ in range(5)]}
    mock_build.return_value.commentThreads.return_value.list.return_value.execute.return_value = mock_response

    comments = fetch_comments("abc123", comment_limit=5)
    assert len(comments) <= 5
```

### 3.3 集計処理（aggregate/summarizer.py）

| No | テスト内容 | 入力 | 期待出力 |
|----|-----------|------|---------|
| 1 | 高positive | comments: positive=0.9が10件 | positive_score ≈ 0.9 |
| 2 | 混合 | 様々なスコア分布 | 平均値が正しく算出 |
| 3 | コメント0件 | comments: [] | total_comments=0, score=0 |

```python
# tests/test_summarizer.py
def test_aggregate_all_positive():
    video = {"video_id": "abc123"}
    comments = [{"sentiment": {"positive": 0.9, "negative": 0.05, "neutral": 0.05}} for _ in range(10)]

    result = aggregate_video(video, comments)

    assert result["total_comments"] == 10
    assert result["positive_score"] > 0.8

def test_aggregate_mixed():
    video = {"video_id": "abc123"}
    comments = [
        {"sentiment": {"positive": 0.8, "negative": 0.1, "neutral": 0.1}},
        {"sentiment": {"positive": 0.2, "negative": 0.7, "neutral": 0.1}},
    ]

    result = aggregate_video(video, comments)

    assert 0.4 < result["positive_score"] < 0.6
    assert 0.3 < result["negative_score"] < 0.5

def test_aggregate_empty():
    video = {"video_id": "abc123"}
    comments = []

    result = aggregate_video(video, comments)

    assert result["total_comments"] == 0
```

## 4. モック設計

### 4.1 モック対象一覧

| 対象 | モック方法 | 用途 |
|-----|-----------|------|
| YouTube Data API | `unittest.mock.patch` | API呼び出しの代替 |
| Transformersモデル | `unittest.mock.patch` | モデル推論の代替 |

### 4.2 YouTube Data API モック

`googleapiclient.discovery.build` をパッチし、メソッドチェーンをMagicMockで再現する。

```python
# tests/test_fetch.py
from unittest.mock import patch, MagicMock

@patch('fetch.youtube.build')
def test_fetch_video(mock_build):
    # モックレスポンスを設定
    mock_response = {
        'items': [{
            'id': 'dQw4w9WgXcQ',
            'snippet': {
                'title': 'Test Video',
                'channelId': 'UC123',
                'channelTitle': 'Test Channel',
                'publishedAt': '2025-01-01T00:00:00Z'
            },
            'statistics': {
                'viewCount': '1000',
                'likeCount': '100',
                'commentCount': '10'
            }
        }]
    }

    # メソッドチェーンのモック
    mock_build.return_value.videos.return_value.list.return_value.execute.return_value = mock_response

    result = fetch_video('dQw4w9WgXcQ')

    assert result['id'] == 'dQw4w9WgXcQ'
    assert result['snippet']['title'] == 'Test Video'

@patch('fetch.youtube.build')
def test_fetch_video_api_error(mock_build):
    """APIクォータ超過のテスト"""
    from googleapiclient.errors import HttpError

    mock_build.return_value.videos.return_value.list.return_value.execute.side_effect = \
        HttpError(resp={'status': '403'}, content=b'quota exceeded')

    with pytest.raises(HttpError):
        fetch_video('abc123')
```

### 4.3 Transformers モック

```python
# tests/test_analyzer.py
from unittest.mock import patch, MagicMock
import torch

@patch('sentiment.analyzer._ja_model_1')
@patch('sentiment.analyzer._ja_tokenizer_1')
def test_classify_with_mock(mock_tokenizer, mock_model):
    """モデル推論のモック"""
    # トークナイザーのモック
    mock_tokenizer.return_value = {'input_ids': torch.tensor([[1, 2, 3]])}

    # モデル出力のモック（logits形式）
    mock_output = MagicMock()
    mock_output.logits = torch.tensor([[0.1, 0.1, 0.8]])  # pos高め
    mock_model.return_value = mock_output

    result = classify_comment('素晴らしい動画')

    assert result['positive'] > 0.5
    assert 'language' in result

def test_classify_fallback_to_rules():
    """モデル未ロード時はルールベースにフォールバック"""
    result = classify_comment('最高！')
    # ルールベースでも結果が返る
    assert 'positive' in result
    assert 'negative' in result
    assert 'neutral' in result
```

### 4.4 テストフィクスチャ（conftest.py）

共通のモック設定をフィクスチャ化する。

```python
# tests/conftest.py
import pytest
from unittest.mock import patch, MagicMock

@pytest.fixture
def mock_youtube_api():
    """YouTube API モックフィクスチャ"""
    with patch('fetch.youtube.build') as mock_build:
        mock_api = MagicMock()
        mock_build.return_value = mock_api
        yield mock_api

# 使用例
def test_with_fixtures(mock_youtube_api):
    mock_youtube_api.videos.return_value.list.return_value.execute.return_value = {...}
    # テスト実行
```

### 4.5 テストデータ管理

```
tests/
├── conftest.py           # 共通フィクスチャ
├── fixtures/
│   ├── video_response.json      # APIレスポンスサンプル
│   └── comments_response.json   # コメントレスポンスサンプル
└── test_*.py
```

```python
# フィクスチャJSONの読み込み
import json
from pathlib import Path

def load_fixture(name: str) -> dict:
    path = Path(__file__).parent / 'fixtures' / name
    return json.loads(path.read_text())

def test_with_fixture_file(mock_youtube_api):
    mock_youtube_api.videos.return_value.list.return_value.execute.return_value = \
        load_fixture('video_response.json')
```
